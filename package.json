{
  "name": "deconstruct",
  "version": "1.0.0",
  "private": true,
  "description": "AI problem deconstruction + schedule-aware routine builder (monorepo)",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @deconstruct/shared && npm run build -w @deconstruct/scheduler && npm run build -w @deconstruct/api",
    "start": "node apps/api/dist/index.js",
    "dev": "npm run build -w @deconstruct/shared && npm run build -w @deconstruct/scheduler && npm run dev -w @deconstruct/api",
    "test": "npm run test -w @deconstruct/scheduler && npm run test -w @deconstruct/api",
    "typecheck": "npm run typecheck -w @deconstruct/api",
    "prisma:generate": "prisma generate --schema apps/api/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema apps/api/prisma/schema.prisma",
    "prisma:deploy": "prisma migrate deploy --schema apps/api/prisma/schema.prisma"
  },
  "dependencies": {
    "prisma": "^6.19.3"
  }
}
